# Changelog

All notable changes to chajá will be documented in this file. The
format is maintained by [release-please](https://github.com/googleapis/release-please);
each merged `feat:` or `fix:` commit on `main` produces a release PR
that updates this file.

## [Unreleased]

The first release is pending. The Release Notes tab is fully functional
once release-please generates the first entry — until then, this empty
section is what `Help → Release Notes` will render.
